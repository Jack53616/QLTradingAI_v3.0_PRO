/* ============================================ */
/* QL TRADING AI - PROFESSIONAL UI */
/* ============================================ */

:root {
  /* Colors */
  --primary: #8250ff;
  --primary-dark: #522acb;
  --accent: #00ffd5;
  --accent-dark: #00d4b8;
  --bg-dark: #0a0814;
  --bg-darker: #05040c;
  --glass: rgba(26, 24, 36, 0.8);
  --glass-border: rgba(255, 255, 255, 0.1);
  --text-primary: #e9e7ff;
  --text-secondary: #9b97bd;
  --success: #1ecb78;
  --danger: #ff3b63;
  --warning: #ffb800;
  
  /* Spacing */
  --spacing-xs: 4px;
  --spacing-sm: 8px;
  --spacing-md: 16px;
  --spacing-lg: 24px;
  --spacing-xl: 32px;
  
  /* Border Radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  
  /* Shadows */
  --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.1);
  --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.2);
  --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.3);
  --shadow-glow: 0 0 30px rgba(130, 80, 255, 0.3);
  
  /* Transitions */
  --transition-fast: 0.15s ease;
  --transition-normal: 0.3s ease;
  --transition-slow: 0.5s ease;
}

/* ============================================ */
/* RESET & BASE */
/* ============================================ */

* {
  box-sizing: border-box;
  margin: 0;
  padding: 0;
}

html, body {
  height: 100%;
  min-height: 100vh;
  overflow-x: hidden;
  max-width: 100vw;
}

body {
  font-family: 'Tajawal', 'Poppins', -apple-system, BlinkMacSystemFont, sans-serif;
  background: var(--bg-darker);
  background-image: 
    radial-gradient(circle at 20% 10%, rgba(130, 80, 255, 0.15) 0%, transparent 50%),
    radial-gradient(circle at 80% 90%, rgba(0, 255, 213, 0.1) 0%, transparent 50%);
  background-attachment: fixed;
  color: var(--text-primary);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  line-height: 1.6;
}

/* ============================================ */
/* UTILITY CLASSES */
/* ============================================ */

.hidden {
  display: none !important;
}

.container {
  max-width: 900px;
  margin: 0 auto;
  padding: 0 var(--spacing-md);
}

/* ============================================ */
/* GLASS MORPHISM CARDS */
/* ============================================ */

.card-glass {
  background: var(--glass);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-lg);
  backdrop-filter: blur(20px) saturate(180%);
  box-shadow: var(--shadow-md);
  transition: transform var(--transition-normal), box-shadow var(--transition-normal);
}

.card-glass:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-lg);
}

.card-glow {
  box-shadow: 
    inset 0 0 0 1px rgba(130, 80, 255, 0.3),
    0 0 30px rgba(130, 80, 255, 0.2),
    0 0 60px rgba(130, 80, 255, 0.1);
}

/* ============================================ */
/* SPLASH SCREEN */
/* ============================================ */

.splash {
  position: fixed;
  inset: 0;
  z-index: 10000;
  display: flex;
  align-items: center;
  justify-content: center;
  background: linear-gradient(135deg, var(--bg-darker) 0%, #1a1230 50%, var(--bg-darker) 100%);
  animation: splashFadeOut 1s ease 2s forwards;
}

.splash-content {
  text-align: center;
  animation: splashPop 0.6s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.logo-container {
  position: relative;
  width: 120px;
  height: 120px;
  margin: 0 auto var(--spacing-lg);
}

.logo-main {
  width: 100%;
  height: 100%;
  filter: drop-shadow(0 0 20px rgba(130, 80, 255, 0.6));
  animation: logoFloat 3s ease-in-out infinite;
}

.logo-glow {
  position: absolute;
  inset: -20px;
  background: radial-gradient(circle, rgba(130, 80, 255, 0.4) 0%, transparent 70%);
  animation: glowPulse 2s ease-in-out infinite;
  z-index: -1;
}

.splash-title {
  font-size: 32px;
  font-weight: 900;
  background: linear-gradient(135deg, var(--primary) 0%, var(--accent) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
  margin-bottom: var(--spacing-sm);
}

.splash-subtitle {
  color: var(--text-secondary);
  font-size: 16px;
  margin-bottom: var(--spacing-lg);
}

.splash-loader {
  width: 200px;
  height: 4px;
  background: rgba(255, 255, 255, 0.1);
  border-radius: 2px;
  overflow: hidden;
  margin: 0 auto;
}

.loader-bar {
  height: 100%;
  background: linear-gradient(90deg, var(--primary), var(--accent));
  border-radius: 2px;
  animation: loaderSlide 1.5s ease-in-out infinite;
}

@keyframes splashPop {
  0% { opacity: 0; transform: scale(0.8) translateY(20px); }
  100% { opacity: 1; transform: scale(1) translateY(0); }
}

@keyframes splashFadeOut {
  to { opacity: 0; visibility: hidden; pointer-events: none; }
}

@keyframes logoFloat {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-10px); }
}

@keyframes glowPulse {
  0%, 100% { opacity: 0.4; }
  50% { opacity: 0.8; }
}

@keyframes loaderSlide {
  0% { transform: translateX(-100%); }
  100% { transform: translateX(200%); }
}

/* ============================================ */
/* GATE (LOGIN SCREEN) */
/* ============================================ */

.gate {
  position: fixed;
  inset: 0;
  z-index: 9999;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--spacing-md);
  overflow-y: auto;
}

.gate-bg {
  position: absolute;
  inset: 0;
  background: 
    radial-gradient(circle at 30% 20%, rgba(130, 80, 255, 0.2) 0%, transparent 50%),
    radial-gradient(circle at 70% 80%, rgba(0, 255, 213, 0.15) 0%, transparent 50%),
    linear-gradient(135deg, var(--bg-darker) 0%, #1a1230 100%);
  z-index: -1;
}

.gate-content {
  width: 100%;
  max-width: 480px;
  animation: gateSlideUp 0.5s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.gate-card {
  padding: var(--spacing-xl);
}

.gate-header {
  text-align: center;
  margin-bottom: var(--spacing-lg);
}

.gate-logo {
  width: 80px;
  height: 80px;
  margin: 0 auto var(--spacing-md);
  padding: var(--spacing-md);
  background: linear-gradient(135deg, rgba(130, 80, 255, 0.2), rgba(0, 255, 213, 0.1));
  border-radius: 50%;
  border: 2px solid var(--glass-border);
}

.gate-logo-img {
  width: 100%;
  height: 100%;
  filter: drop-shadow(0 0 10px rgba(130, 80, 255, 0.5));
}

.gate-title {
  font-size: 28px;
  font-weight: 900;
  margin-bottom: var(--spacing-sm);
  background: linear-gradient(135deg, var(--primary), var(--accent));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.gate-subtitle {
  color: var(--text-secondary);
  font-size: 14px;
}

.gate-form {
  margin-bottom: var(--spacing-lg);
}

.input-group {
  position: relative;
  margin-bottom: var(--spacing-md);
}

.input-field {
  width: 100%;
  padding: 14px 48px 14px 16px;
  background: rgba(14, 13, 20, 0.6);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  font-size: 15px;
  font-family: inherit;
  transition: all var(--transition-normal);
  outline: none;
}

.input-field:focus {
  border-color: var(--primary);
  box-shadow: 0 0 0 3px rgba(130, 80, 255, 0.2);
}

.input-field::placeholder {
  color: var(--text-secondary);
}

.input-key {
  font-weight: 700;
  letter-spacing: 1px;
  text-transform: uppercase;
}

.input-icon {
  position: absolute;
  left: 16px;
  top: 50%;
  transform: translateY(-50%);
  font-size: 20px;
  pointer-events: none;
}

.gate-actions {
  display: flex;
  gap: var(--spacing-sm);
  margin-top: var(--spacing-lg);
}

.btn {
  position: relative;
  padding: 14px 24px;
  border: none;
  border-radius: var(--radius-md);
  font-size: 15px;
  font-weight: 700;
  font-family: inherit;
  cursor: pointer;
  transition: all var(--transition-normal);
  overflow: hidden;
  outline: none;
}

.btn-primary {
  flex: 1;
  background: linear-gradient(135deg, var(--primary) 0%, var(--primary-dark) 100%);
  color: white;
  box-shadow: 0 8px 24px rgba(130, 80, 255, 0.4);
}

.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 12px 32px rgba(130, 80, 255, 0.5);
}

.btn-primary:active {
  transform: translateY(0);
}

.btn-secondary {
  padding: 14px 20px;
  background: rgba(14, 13, 20, 0.6);
  border: 1px solid var(--glass-border);
  color: var(--text-primary);
}

.btn-secondary:hover {
  background: rgba(20, 18, 28, 0.8);
  border-color: var(--primary);
}

.btn-glow {
  box-shadow: 
    0 8px 24px rgba(130, 80, 255, 0.4),
    inset 0 0 0 1px rgba(255, 255, 255, 0.1);
}

.btn-shine {
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
  animation: btnShine 3s ease-in-out infinite;
}

.btn-text {
  position: relative;
  z-index: 1;
}

.btn-sm {
  padding: 8px 16px;
  font-size: 13px;
}

.btn-block {
  width: 100%;
}

@keyframes btnShine {
  0% { left: -100%; }
  50%, 100% { left: 200%; }
}

.gate-toast {
  min-height: 24px;
  margin-top: var(--spacing-md);
  padding: var(--spacing-sm);
  border-radius: var(--radius-sm);
  font-size: 13px;
  text-align: center;
  transition: all var(--transition-normal);
}

.gate-toast.error {
  background: rgba(255, 59, 99, 0.2);
  border: 1px solid rgba(255, 59, 99, 0.3);
  color: #ffd2da;
}

.gate-toast.success {
  background: rgba(30, 203, 120, 0.2);
  border: 1px solid rgba(30, 203, 120, 0.3);
  color: #d2ffd9;
}

.gate-footer {
  text-align: center;
  padding-top: var(--spacing-lg);
  border-top: 1px solid var(--glass-border);
}

.gate-footer-text {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: var(--spacing-sm);
  color: var(--text-secondary);
  font-size: 13px;
}

@keyframes gateSlideUp {
  0% { opacity: 0; transform: translateY(40px); }
  100% { opacity: 1; transform: translateY(0); }
}

/* ============================================ */
/* TOP BAR */
/* ============================================ */

.top-bar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 1000;
  background: linear-gradient(180deg, rgba(19, 16, 32, 0.95) 0%, rgba(19, 16, 32, 0.8) 100%);
  border-bottom: 1px solid var(--glass-border);
  backdrop-filter: blur(20px) saturate(180%);
  padding: var(--spacing-sm) 0;
}

.top-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--spacing-sm);
}

.brand {
  display: flex;
  align-items: center;
  gap: var(--spacing-sm);
}

.brand-logo {
  width: 32px;
  height: 32px;
  filter: drop-shadow(0 0 8px rgba(130, 80, 255, 0.5));
}

.brand-text {
  font-size: 20px;
  font-weight: 900;
  background: linear-gradient(135deg, var(--primary), var(--accent));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.top-actions {
  display: flex;
  gap: var(--spacing-sm);
}

.icon-btn {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: rgba(14, 13, 20, 0.6);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-md);
  font-size: 18px;
  cursor: pointer;
  transition: all var(--transition-normal);
}

.icon-btn:hover {
  background: rgba(20, 18, 28, 0.8);
  border-color: var(--primary);
  transform: translateY(-2px);
}

/* ============================================ */
/* TABS */
/* ============================================ */

.tabs-container {
  display: flex;
  gap: var(--spacing-xs);
  padding: var(--spacing-xs);
  background: rgba(14, 13, 20, 0.4);
  border-radius: var(--radius-md);
  overflow-x: auto;
  scrollbar-width: none;
}

.tabs-container::-webkit-scrollbar {
  display: none;
}

.tab-btn {
  flex: 1;
  min-width: 80px;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
  padding: var(--spacing-sm);
  background: transparent;
  border: 1px solid transparent;
  border-radius: var(--radius-sm);
  color: var(--text-secondary);
  font-size: 11px;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  transition: all var(--transition-normal);
}

.tab-btn.active {
  background: linear-gradient(135deg, var(--primary), var(--primary-dark));
  border-color: rgba(255, 255, 255, 0.1);
  color: white;
  box-shadow: 0 4px 16px rgba(130, 80, 255, 0.4);
}

.tab-icon {
  font-size: 20px;
}

/* ============================================ */
/* CONTENT */
/* ============================================ */

.content {
  padding-top: 140px;
  padding-bottom: var(--spacing-xl);
  min-height: 100vh;
}

.tab-content {
  display: none;
  animation: tabFadeIn 0.3s ease;
}

.tab-content.show {
  display: block;
}

@keyframes tabFadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

.section-title {
  font-size: 20px;
  font-weight: 800;
  margin-bottom: var(--spacing-md);
}

/* ============================================ */
/* BALANCE CARD */
/* ============================================ */

.balance-card {
  padding: var(--spacing-lg);
  margin-bottom: var(--spacing-lg);
}

.balance-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--spacing-sm);
}

.balance-label {
  color: var(--text-secondary);
  font-size: 13px;
}

.level-badge {
  padding: 4px 12px;
  background: linear-gradient(135deg, rgba(255, 184, 0, 0.2), rgba(255, 140, 0, 0.1));
  border: 1px solid rgba(255, 184, 0, 0.3);
  border-radius: 12px;
  color: #ffd98a;
  font-size: 12px;
  font-weight: 700;
}

.balance-main {
  display: flex;
  align-items: baseline;
  gap: var(--spacing-md);
  margin-bottom: var(--spacing-md);
}

.balance-value {
  font-size: 42px;
  font-weight: 900;
  background: linear-gradient(135deg, white, var(--accent));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}

.balance-change {
  font-size: 16px;
  font-weight: 700;
  color: var(--success);
}

.balance-chart {
  height: 60px;
  margin-bottom: var(--spacing-md);
  opacity: 0.6;
}

.balance-chart svg {
  width: 100%;
  height: 100%;
}

.balance-chart path {
  fill: none;
  stroke: var(--accent);
  stroke-width: 2;
  filter: drop-shadow(0 0 8px rgba(0, 255, 213, 0.6));
}

.balance-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-top: var(--spacing-md);
  border-top: 1px solid var(--glass-border);
}

.balance-info {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.info-label {
  color: var(--text-secondary);
  font-size: 12px;
}

.info-value {
  color: var(--text-primary);
  font-size: 14px;
  font-weight: 700;
}

/* ============================================ */
/* STATS GRID */
/* ============================================ */

.stats-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-lg);
}

.stat-card {
  padding: var(--spacing-md);
  text-align: center;
}

.stat-icon {
  font-size: 28px;
  margin-bottom: var(--spacing-sm);
}

.stat-content {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.stat-label {
  color: var(--text-secondary);
  font-size: 11px;
}

.stat-value {
  color: var(--text-primary);
  font-size: 16px;
  font-weight: 800;
}

/* ============================================ */
/* MARKETS GRID */
/* ============================================ */

.markets-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
  gap: var(--spacing-md);
}

.market-card {
  padding: var(--spacing-md);
  cursor: pointer;
  transition: all var(--transition-normal);
}

.market-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-glow);
}

.market-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--spacing-sm);
}

.market-name {
  font-size: 14px;
  font-weight: 700;
  color: var(--text-secondary);
}

.market-change {
  font-size: 12px;
  font-weight: 700;
}

.market-change.up {
  color: var(--success);
}

.market-change.down {
  color: var(--danger);
}

.market-price {
  font-size: 20px;
  font-weight: 900;
  color: var(--text-primary);
}

/* ============================================ */
/* TRADES & ACTIVITY */
/* ============================================ */

.trades-list, .activity-feed {
  display: flex;
  flex-direction: column;
  gap: var(--spacing-sm);
}

.trade-item, .activity-item {
  padding: var(--spacing-md);
  background: var(--glass);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-md);
  font-size: 14px;
  transition: all var(--transition-normal);
}

.trade-item:hover, .activity-item:hover {
  border-color: var(--primary);
  transform: translateX(-4px);
}

/* ============================================ */
/* BOTTOM SHEET */
/* ============================================ */

.bottom-sheet {
  position: fixed;
  inset: 0;
  z-index: 9998;
  display: flex;
  align-items: flex-end;
  animation: sheetFadeIn 0.3s ease;
}

.sheet-overlay {
  position: absolute;
  inset: 0;
  background: rgba(0, 0, 0, 0.7);
  backdrop-filter: blur(4px);
}

.sheet-content {
  position: relative;
  width: 100%;
  max-width: 600px;
  margin: 0 auto;
  padding: var(--spacing-lg);
  border-radius: var(--radius-xl) var(--radius-xl) 0 0;
  animation: sheetSlideUp 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
}

.sheet-handle {
  width: 40px;
  height: 4px;
  background: var(--glass-border);
  border-radius: 2px;
  margin: 0 auto var(--spacing-md);
}

.sheet-title {
  font-size: 20px;
  font-weight: 800;
  text-align: center;
  margin-bottom: var(--spacing-lg);
}

.sheet-body {
  margin-bottom: var(--spacing-lg);
}

.method-btn {
  width: 100%;
  display: flex;
  align-items: center;
  gap: var(--spacing-md);
  padding: var(--spacing-md);
  background: rgba(14, 13, 20, 0.6);
  border: 1px solid var(--glass-border);
  border-radius: var(--radius-md);
  color: var(--text-primary);
  font-size: 15px;
  font-family: inherit;
  cursor: pointer;
  transition: all var(--transition-normal);
  margin-bottom: var(--spacing-sm);
}

.method-btn:hover {
  background: rgba(20, 18, 28, 0.8);
  border-color: var(--primary);
  transform: translateX(4px);
}

.method-icon {
  font-size: 24px;
}

.sheet-cancel {
  margin-top: var(--spacing-md);
}

@keyframes sheetFadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes sheetSlideUp {
  from { transform: translateY(100%); }
  to { transform: translateY(0); }
}

/* ============================================ */
/* RESPONSIVE */
/* ============================================ */

@media (max-width: 480px) {
  .stats-grid {
    grid-template-columns: 1fr;
  }
  
  .balance-value {
    font-size: 36px;
  }
  
  .gate-card {
    padding: var(--spacing-lg);
  }
}
